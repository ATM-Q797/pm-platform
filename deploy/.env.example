# ============================================================
# 智能终端研发项目管理平台 — 环境变量配置
# ============================================================
# 用法：复制本文件为 .env，修改后保存
#   cp deploy/.env.example deploy/.env
#   vim deploy/.env
# ============================================================

# ----------------------------------------------------------
# [必填] PostgreSQL 数据库
# ----------------------------------------------------------
POSTGRES_DB=pm_platform
POSTGRES_USER=pm_user
POSTGRES_PASSWORD=CHANGE_ME_TO_A_STRONG_PASSWORD

# ----------------------------------------------------------
# [必填] JWT 密钥（至少 32 字符的随机字符串）
# 生成方式: python3 -c "import secrets; print(secrets.token_urlsafe(48))"
# ----------------------------------------------------------
JWT_SECRET_KEY=

# ----------------------------------------------------------
# [必填] 前端访问域名（用于 CORS 白名单）
# 多个域名用逗号分隔，如: https://pm.example.com,https://www.pm.example.com
# ----------------------------------------------------------
CORS_ORIGINS=http://localhost

# ----------------------------------------------------------
# Cookie 安全设置
# 启用 HTTPS 后设为 true
# ----------------------------------------------------------
COOKIE_SECURE=false

# ----------------------------------------------------------
# 服务端口
# ----------------------------------------------------------
HTTP_PORT=80
HTTPS_PORT=443
BACKEND_PORT=8000
POSTGRES_PORT=5432

# ----------------------------------------------------------
# 后端性能
# ----------------------------------------------------------
WORKERS=4
TOKEN_EXPIRE_HOURS=24
DB_POOL_SIZE=10
DB_MAX_OVERFLOW=20
LOG_LEVEL=info
